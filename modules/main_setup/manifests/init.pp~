class main_setup{
    exec { "apt-get update":
        command => "/usr/bin/apt-get update"
    }

    package {
        [
        'build-essential',
        'vim',
        'curl',
        'git'
      ]:
          require => Exec["apt-get update"],
          ensure  => 'installed',
  }
}
